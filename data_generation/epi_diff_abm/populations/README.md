# EpiABM Populations Directory

This directory is intentionally empty in the code release except for this file.

The upstream population-generation scripts create one package per county, for
example:

```text
populations/pop01045/
```

These generated population packages are required for calibration and rollout but
are not committed to the repository.
